# ⚽ YOLOv8 기반 축구 영상 객체 탐지 (Football Object Detection)

## 📌 프로젝트 개요 (Project Overview)
이 프로젝트는 **YOLOv8** 딥러닝 모델을 활용하여 축구 경기 영상에서 **공(Ball), 선수(Player), 심판(Referee)** 등 주요 객체를 실시간으로 탐지하는 AI 모델을 구축하는 프로젝트입니다.
넓은 경기장을 촬영한 영상 특성상, 매우 작게 보이는 **축구공**과 **선수**들을 빠르고 정확하게 구분해내는 것을 목표로 합니다.

## 🛠️ 기술 스택 (Tech Stack)
- **Model:** YOLOv8 (Ultralytics)
- **Environment:** Google Colab (T4 GPU)
- **Library:** PyTorch, OpenCV, Pandas
- **Data Format:** JSON (LabelMe 형식) → YOLO 포맷 (.txt)으로 전처리 수행

## 📂 데이터셋 정보 (Dataset Info)
- **데이터 출처:** 핏투게더 (Fittogether) 축구 영상 데이터셋
- **탐지 클래스 (5개):**
  0. `ball` (축구공)
  1. `players` (선수)
  2. `others` (코치 등 기타 인물)
  3. `goalkeeper` (골키퍼)
  4. `referee` (심판)

## 🧪 실험 내용 (Experiments)
실시간 탐지 성능(Speed)과 정확도(Accuracy)의 최적 균형을 찾기 위해 두 가지 실험을 진행했습니다.

| 실험 (Exp) | 모델 (Model) | 학습 횟수 (Epochs) | 이미지 크기 (ImgSz) | 배치 크기 (Batch) | 실험 결과 요약 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **Exp 1** | YOLOv8s (Small) | 10 | 640 | 16 | 정확도는 준수하나, 추론 속도가 실시간 처리에 다소 느림 |
| **Exp 2** | **YOLOv8n (Nano)** | **30** | **640** | **32** | **학습 및 추론 속도가 매우 빠르며, 실시간 탐지에 적합한 성능 확보** |

> **결론:** 실시간 영상 분석이 중요한 축구 도메인 특성상, 속도가 빠르고 학습 효율이 높은 **Exp 2 (Nano 모델, 30 Epochs)** 모델을 최종 선정하였습니다.

## 📊 학습 결과 (Results)
### 1. 학습 지표 (Metrics)
<img width="2400" height="1200" alt="results (1)" src="https://github.com/user-attachments/assets/3f1f36c4-c796-4246-87b6-8bf0b78bcb89" />

### 2. 탐지 예시 (Detection Example)
![val_batch0_labels](https://github.com/user-attachments/assets/4bbe0a60-c091-4b23-93be-22cbe86f06dd)

## 🚀 실행 방법 (How to Run)

### 1. 라이브러리 설치
```
pip install ultralytics
```
### 2. 모델 학습 (Train)
```
from ultralytics import YOLO

# Nano 모델로 30 Epoch 학습 수행
model = YOLO('yolov8n.pt')
model.train(data='football.yaml', epochs=30, imgsz=640)
```
### 3. 추론 및 테스트 (Inference)
```
model = YOLO('best.pt')  # 학습된 가중치 로드
model.predict('test_video.mp4', save=True)

📂 파일 구조 (File Structure)
├── football.yaml       # 데이터셋 설정 파일 (클래스 및 경로 정의)
├── football_detection.ipynb  # 전체 프로젝트 실행 코드 (Colab)
├── best_nano_ep30.pt   # 학습 완료된 모델 가중치 (Exp 2 결과)
├── README.md           # 프로젝트 설명 문서
└── results/            # 결과 그래프 및 테스트 이미지 저장 폴더
```
